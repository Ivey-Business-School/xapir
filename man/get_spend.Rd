% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_spend.R
\name{get_spend}
\alias{get_spend}
\title{Get Spend}
\usage{
get_spend(days = 7, bearer_token = Sys.getenv("X_BEARER_TOKEN"))
}
\arguments{
\item{days}{\code{numeric}; how many days back to report, between 1 and
90. Default 7.}

\item{bearer_token}{A string containing the bearer token for authenticating
with the X API. By default, this argument retrieves the token from the
environment variable \code{X_BEARER_TOKEN} (via \code{Sys.getenv("X_BEARER_TOKEN")}).
Adding your bearer token to your \code{.Renviron} file keeps it out of your
scripts and available in every session.}
}
\value{
A tibble with one row per day, oldest first: \code{date} (Date),
\code{posts} (integer, posts read that day) and \code{dollars} (double, \code{posts}
times the post price). A period with no usage gives zero rows with the
same columns.
}
\description{
Turns the daily post reads that \code{get_usage()} reports (via the
\href{https://docs.x.com/x-api/usage/get-usage}{get usage endpoint}) into a
day-by-day estimate of what they cost at the post price, and prints one
summary line. This is your own usage, so no cost line is printed: the
pricing page does not list the usage endpoints as billed.

The estimate counts post reads only, because the usage endpoint reports
nothing else: user reads, follower reads, counts and writes do not
appear here. It is also a ceiling. Reads of your own account's data bill
at the owned rate, and a post read twice on the same UTC day is billed
once, so the real bill is lower. \code{get_usage_credits()} shows the actual
balance.
}
\examples{
\dontrun{
spend <- get_spend()
#> Read 1,430 posts in the last 7 days, about $7.15 at the post price.

spend <- get_spend(days = 30)
sum(spend$dollars)
}
}
