% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_spaces.R
\name{get_spaces}
\alias{get_spaces}
\title{Get Spaces by IDs or by Creator IDs}
\usage{
get_spaces(
  space_ids = NULL,
  user_ids = NULL,
  bearer_token = Sys.getenv("X_BEARER_TOKEN"),
  space_fields = default_space_fields()
)
}
\arguments{
\item{space_ids}{A character vector of up to 100 space ids, each a short
string of letters and digits such as \code{"1DXxyRYNejbKM"}.}

\item{user_ids}{A character vector of up to 100 user ids, each a string
of digits. Keep ids as text: as numbers they lose digits. Returns the
spaces those users created, so one user id can give several rows.}

\item{bearer_token}{A string containing the bearer token for authenticating
with the X API. By default, this argument retrieves the token from the
environment variable \code{X_BEARER_TOKEN} (via \code{Sys.getenv("X_BEARER_TOKEN")}).
Adding your bearer token to your \code{.Renviron} file keeps it out of your
scripts and available in every session.}

\item{space_fields}{\code{character}, \code{vector}; the fields to return
for each space. The default asks for everything the table reads.}
}
\value{
A tibble with one row per space: \code{space_id}, \code{title}, \code{state}
(\code{"live"}, \code{"scheduled"} or \code{"ended"}), \code{creator_id}, \code{created_at},
\code{scheduled_start}, \code{started_at} and \code{ended_at} (POSIXct, UTC), \code{lang},
\code{is_ticketed}, \code{participant_count}, \code{subscriber_count}, \code{host_ids} and
\code{speaker_ids} (list-columns, one character vector of user ids per row).
When no space is found, the same columns with no rows.
}
\description{
Retrieves up to 100 spaces by their ids via the
\href{https://docs.x.com/x-api/spaces/get-spaces-by-ids}{get spaces by IDs endpoint},
or every space created by up to 100 users via the
\href{https://docs.x.com/x-api/spaces/get-spaces-by-creator-ids}{get spaces by creator IDs endpoint}.
Give exactly one of \code{space_ids} and \code{user_ids}. Every space returned is
billed, so the function says what the call can cost before it reads
anything. An app bearer token is enough.

An id the API cannot find does not stop the call: the spaces it did find
are returned, and one warning names each id that was not.
}
\examples{
\dontrun{
spaces <- get_spaces(space_ids = c("1DXxyRYNejbKM", "1nAKErYNqlpxL"))
spaces <- get_spaces(user_ids = c("783214", "2244994945"))
}
}
