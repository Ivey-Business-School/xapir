% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_communities.R
\name{get_community}
\alias{get_community}
\title{Get Community by ID}
\usage{
get_community(
  community_id,
  bearer_token = Sys.getenv("X_BEARER_TOKEN"),
  community_fields = default_community_fields()
)
}
\arguments{
\item{community_id}{The community's id, as a string of digits. Keep ids
as text: as numbers they lose digits.}

\item{bearer_token}{A string containing the bearer token for authenticating
with the X API. By default, this argument retrieves the token from the
environment variable \code{X_BEARER_TOKEN} (via \code{Sys.getenv("X_BEARER_TOKEN")}).
Adding your bearer token to your \code{.Renviron} file keeps it out of your
scripts and available in every session.}

\item{community_fields}{\code{character}, \code{vector}; the fields to
return for the community. The default asks for everything the table
reads.}
}
\value{
A tibble with one row: \code{community_id}, \code{name}, \code{description},
\code{access} (\code{"Public"} or \code{"Closed"}), \code{join_policy} (\code{"Open"},
\code{"RestrictedJoinRequestsDisabled"}, ...), \code{member_count} and
\code{created_at} (POSIXct, UTC).
}
\description{
Retrieves the details of one community by its id via the
\href{https://docs.x.com/x-api/communities/get-community-by-id}{get community by ID endpoint}.
Every community returned is billed, so the function says what the call
can cost before it reads anything. An app bearer token is enough. An id
the API cannot find stops the call with the API's reason.
}
\examples{
\dontrun{
community <- get_community("1493446837214187523")
}
}
