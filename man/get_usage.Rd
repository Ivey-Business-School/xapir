% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_usage.R
\name{get_usage}
\alias{get_usage}
\title{Get Usage}
\usage{
get_usage(days = 7, bearer_token = Sys.getenv("X_BEARER_TOKEN"))
}
\arguments{
\item{days}{\code{numeric}; how many days of daily usage to return,
between 1 and 90. Default 7.}

\item{bearer_token}{A string containing the bearer token for authenticating
with the X API. By default, this argument retrieves the token from the
environment variable \code{X_BEARER_TOKEN} (via \code{Sys.getenv("X_BEARER_TOKEN")}).
Adding your bearer token to your \code{.Renviron} file keeps it out of your
scripts and available in every session.}
}
\value{
A tibble with one row: \code{project_id} (character), \code{project_cap}
(integer, posts a month), \code{project_usage} (integer, posts read so far
this cycle) and \code{cap_reset_day} (integer, the day of the month the
count restarts). The daily breakdown is attached as the attribute
\code{"daily"}: a tibble with \code{date} (Date) and \code{usage} (integer), one row
per day, oldest first. Read it with \code{attr(usage, "daily")}.
}
\description{
Returns how many posts your project has read against its monthly cap via
the \href{https://docs.x.com/x-api/usage/get-usage}{get usage endpoint}. This
is your own spend, so no cost line is printed: the pricing page does not
list the usage endpoints as billed.
}
\examples{
\dontrun{
usage <- get_usage()
attr(usage, "daily")
}
}
