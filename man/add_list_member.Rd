% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_member.R
\name{add_list_member}
\alias{add_list_member}
\title{Add List Member}
\usage{
add_list_member(list_id, username = NULL, user_id = NULL)
}
\arguments{
\item{list_id}{The id of the list, as a string.}

\item{username}{Username of the account to add, without the "@" symbol.}

\item{user_id}{The id of the account to add, as a string of digits. When
given, \code{username} must be \code{NULL}.}
}
\value{
Invisibly, the \code{data} list the API returns, \code{list(is_member = TRUE)}. Stops with the API's message when the request is refused.
}
\description{
Adds an account to a list the signed-in account owns via the \href{https://docs.x.com/x-api/lists/add-list-member}{add list member endpoint}. Needs a
user token, so the first call opens a browser window to sign in. The
request is billed, so the function says what it costs before it sends
anything.

Give either \code{username} or \code{user_id}, not both. A handle costs one user
read to turn it into an id before the member is added; pass the id when
you already know it and that read is skipped.
}
\examples{
\dontrun{
add_list_member(list_id = "1146654567674912769", username = "Tesla")
add_list_member(list_id = "1146654567674912769", user_id = "13298072")
}
}
