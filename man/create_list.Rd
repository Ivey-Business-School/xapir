% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_list.R
\name{create_list}
\alias{create_list}
\title{Create List}
\usage{
create_list(name, description = NULL, private = FALSE)
}
\arguments{
\item{name}{The name of the list, 1 to 25 characters.}

\item{description}{An optional description, up to 100 characters.}

\item{private}{\code{TRUE} makes the list private. Default \code{FALSE}.}
}
\value{
Invisibly, the \code{data} list the API returns, \code{list(id = "...", name = "...")}. Keep the \code{id}: every other list function takes it as
\code{list_id}. Stops with the API's message when the request is refused.
}
\description{
Creates a list owned by the signed-in account via the \href{https://docs.x.com/x-api/lists/create-list}{create list endpoint}. Needs a user
token, so the first call opens a browser window to sign in. The request
is billed, so the function says what it costs before it sends anything.
}
\examples{
\dontrun{
new_list <- create_list(name = "EV makers", description = "Who builds EVs")
new_list$id
}
}
