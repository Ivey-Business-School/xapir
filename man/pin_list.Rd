% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pin_list.R
\name{pin_list}
\alias{pin_list}
\title{Pin List}
\usage{
pin_list(list_id)
}
\arguments{
\item{list_id}{The id of the list to pin, as a string.}
}
\value{
Invisibly, the \code{data} list the API returns, \code{list(pinned = TRUE)}.
Stops with the API's message when the request is refused.
}
\description{
Pins a list to the top of the signed-in account's lists via the \href{https://docs.x.com/x-api/users/pin-list}{pin list endpoint}. An account can pin up
to five lists. Needs a user token, so the first call opens a browser
window to sign in. The request is billed, so the function says what it
costs before it sends anything.
}
\examples{
\dontrun{
pin_list(list_id = "1146654567674912769")
}
}
