% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_post_hashtag.R
\name{extract_post_hashtag}
\alias{extract_post_hashtag}
\title{Extract Post Hashtag Data from Timeline}
\usage{
extract_post_hashtag(timeline, include_referenced_posts = TRUE)
}
\arguments{
\item{timeline}{A list containing the timeline data retrieved from the X API.}

\item{include_referenced_posts}{Logical. Whether to include the posts in
\code{includes$tweets} (the posts that were quoted, replied to or reposted).
Defaults to TRUE.}
}
\value{
A tibble with one row per hashtag per post and the columns
\code{post_id} (character), \code{hashtag} (character, without the \verb{#}), \code{start}
and \code{end} (integer positions in the post text). A timeline without
hashtags gives zero rows with the same columns.
}
\description{
Processes the timeline data retrieved from the X API to wrangle the
hashtags in each post and where they sit in the text. Each row is one
hashtag in one post.

Each post appears once, even when it sits in one page's \code{data} and another
page's \code{includes$tweets}. The \code{data} copy wins.
}
\examples{
\dontrun{
timeline <- get_timeline(
  username = "XDevelopers",
  max_results = 100,
  start_time = iso_8601(Sys.Date() - 7)
)
post_hashtag <- extract_post_hashtag(timeline)
}
}
