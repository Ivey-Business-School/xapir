% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_list.R
\name{update_list}
\alias{update_list}
\title{Update List}
\usage{
update_list(list_id, name = NULL, description = NULL, private = NULL)
}
\arguments{
\item{list_id}{The id of the list, as a string.}

\item{name}{A new name, 1 to 25 characters, or \code{NULL} to keep the old one.}

\item{description}{A new description, up to 100 characters, or \code{NULL}.}

\item{private}{\code{TRUE} or \code{FALSE} to change the privacy, or \code{NULL}.}
}
\value{
Invisibly, the \code{data} list the API returns, \code{list(updated = TRUE)}. Stops with the API's message when the request is refused.
}
\description{
Changes the name, description or privacy of a list the signed-in account
owns via the \href{https://docs.x.com/x-api/lists/update-list}{update list endpoint}. Only the arguments
given are sent; the rest stay as they are. Needs a user token, so the
first call opens a browser window to sign in. The request is billed, so
the function says what it costs before it sends anything.
}
\examples{
\dontrun{
update_list(list_id = "1146654567674912769", name = "EV makers 2026")
update_list(list_id = "1146654567674912769", private = TRUE)
}
}
