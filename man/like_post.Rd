% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/like_post.R
\name{like_post}
\alias{like_post}
\title{Like Post}
\usage{
like_post(post_id)
}
\arguments{
\item{post_id}{The id of the post to like, as a string.}
}
\value{
Invisibly, the \code{data} list the API returns, \code{list(liked = TRUE)}.
Stops with the API's message when the like is refused.
}
\description{
Likes a post from the signed-in account via the \href{https://docs.x.com/x-api/users/like-post}{like post endpoint}. Needs a user token,
so the first call opens a browser window to sign in. The request is billed
as one interaction, so the function says what it costs before it sends
anything.
}
\examples{
\dontrun{
like_post(post_id = "20")
}
}
