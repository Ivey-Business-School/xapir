% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_news.R
\name{search_news}
\alias{search_news}
\title{Search News}
\usage{
search_news(
  query,
  max_results = 20,
  max_age_hours = 24,
  bearer_token = Sys.getenv("X_BEARER_TOKEN"),
  news_fields = default_news_fields()
)
}
\arguments{
\item{query}{One search string, such as \code{"electric vehicles"}.}

\item{max_results}{The most stories to return, between 1 and 100.
Default 20. The function stops before any request if the value is
outside that range.}

\item{max_age_hours}{Only stories updated within this many hours, between
1 and 720 (30 days). Default 24.}

\item{bearer_token}{A string containing the bearer token for authenticating
with the X API. By default, this argument retrieves the token from the
environment variable \code{X_BEARER_TOKEN} (via \code{Sys.getenv("X_BEARER_TOKEN")}).
Adding your bearer token to your \code{.Renviron} file keeps it out of your
scripts and available in every session.}

\item{news_fields}{\code{character}, \code{vector}; the fields to return
for each story. The default asks for everything the table reads.}
}
\value{
A tibble with one row per story: \code{news_id}, \code{name} (the
headline), \code{hook}, \code{summary}, \code{category}, \code{disclaimer}, \code{keywords}
(list-column, one character vector per row), \code{contexts} (list-column,
one character vector per row of every entity, topic, ticker and team
the API tagged the story with, flattened from its nested groups) and
\code{updated_at} (POSIXct, UTC). When nothing matches, the same columns
with no rows.
}
\description{
Finds news stories breaking on X that match a search via the
\href{https://docs.x.com/x-api/news/search-news}{search news endpoint}.
Each story is a summary Grok wrote from posts on X, with a headline, a
hook and the entities it mentions. An app bearer token is enough.

News is not on the X API pricing page as of 24 September 2026, so this
function prints no cost line. Check
\url{https://docs.x.com/x-api/getting-started/pricing} before pulling a lot.
}
\examples{
\dontrun{
news <- search_news("electric vehicles", max_results = 10, max_age_hours = 48)
}
}
