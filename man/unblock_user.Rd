% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/block_user.R
\name{unblock_user}
\alias{unblock_user}
\title{Unblock User}
\usage{
unblock_user(target_username = NULL, target_user_id = NULL)
}
\arguments{
\item{target_username}{Username of the account to unblock, without the
"@" symbol.}

\item{target_user_id}{The id of the account to unblock, as a string of
digits. When given, \code{target_username} must be \code{NULL}.}
}
\value{
Invisibly, the \code{data} list the API returns, \code{list(blocking = FALSE)}. Stops with the API's message when the request is refused.
}
\description{
Removes a block from the signed-in account via the \href{https://docs.x.com/x-api/users/unblock-user}{unblock user endpoint}. Needs a user
token, so the first call opens a browser window to sign in. The request
is billed, so the function says what it costs before it sends anything.

Give either \code{target_username} or \code{target_user_id}, not both. A handle
costs one user read to turn it into an id before the request is sent.

The docs list this endpoint as Enterprise only, so on a pay-per-use tier
the API may refuse it. The function passes the API's message on.
}
\examples{
\dontrun{
unblock_user(target_username = "spammer")
}
}
