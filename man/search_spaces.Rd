% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_spaces.R
\name{search_spaces}
\alias{search_spaces}
\title{Search Spaces}
\usage{
search_spaces(
  query,
  state = "all",
  max_results = 100,
  bearer_token = Sys.getenv("X_BEARER_TOKEN"),
  space_fields = default_space_fields()
)
}
\arguments{
\item{query}{One search string, matched against space titles.}

\item{state}{Which spaces to return: \code{"live"}, \code{"scheduled"} or \code{"all"}
(the default).}

\item{max_results}{The most spaces to return, between 1 and 100. Default
100. The function stops before any request if the value is outside that
range.}

\item{bearer_token}{A string containing the bearer token for authenticating
with the X API. By default, this argument retrieves the token from the
environment variable \code{X_BEARER_TOKEN} (via \code{Sys.getenv("X_BEARER_TOKEN")}).
Adding your bearer token to your \code{.Renviron} file keeps it out of your
scripts and available in every session.}

\item{space_fields}{\code{character}, \code{vector}; the fields to return
for each space. The default asks for everything the table reads.}
}
\value{
A tibble with one row per space and the columns described in
\code{\link[=get_spaces]{get_spaces()}}: \code{space_id}, \code{title}, \code{state}, \code{creator_id}, \code{created_at},
\code{scheduled_start}, \code{started_at}, \code{ended_at} (POSIXct, UTC), \code{lang},
\code{is_ticketed}, \code{participant_count}, \code{subscriber_count}, \code{host_ids} and
\code{speaker_ids} (list-columns). When nothing matches, the same columns
with no rows.
}
\description{
Finds live or scheduled spaces whose title matches a search via the
\href{https://docs.x.com/x-api/spaces/search-spaces}{search spaces endpoint}.
Every space returned is billed, so the function says what the call can
cost before it reads anything. An app bearer token is enough.
}
\examples{
\dontrun{
live <- search_spaces("marketing", state = "live", max_results = 20)
}
}
