% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_all_post_count.R
\name{get_all_post_count}
\alias{get_all_post_count}
\title{Get All Post Count}
\usage{
get_all_post_count(
  query,
  start_time = NULL,
  end_time = NULL,
  granularity = "day",
  is_local_tz = TRUE,
  drop_incomplete = TRUE,
  bearer_token = Sys.getenv("X_BEARER_TOKEN")
)
}
\arguments{
\item{query}{The search to be made on X. You can find ways to build
specific queries according to the \href{https://docs.x.com/x-api/posts/search/integrate/build-a-query#types}{X API documentation website}}

\item{start_time}{The earliest date-time from which you want to count
posts. Provide the value in ISO 8601 format (i.e.,
\code{YYYY-MM-DDTHH:mm:ssZ}). The \code{iso_8601()} function will convert a
string, date, or date-time object to the required format (e.g.,
\code{iso_8601("2024-10-10")}). Without it the count starts 30 days ago.}

\item{end_time}{The latest date-time to which you want to count posts.}

\item{granularity}{The period each count covers: \code{"minute"}, \code{"hour"} or
\code{"day"}. The function stops before any request on anything else.}

\item{is_local_tz}{Logical. Convert \code{start} and \code{end} from UTC to the
system time zone.}

\item{drop_incomplete}{Drops the first and last rows, which cover only part
of a \code{granularity} period.}

\item{bearer_token}{A string containing the bearer token for authenticating
with the X API. By default, this argument retrieves the token from the
environment variable \code{X_BEARER_TOKEN} (via \code{Sys.getenv("X_BEARER_TOKEN")}).
Adding your bearer token to your \code{.Renviron} file keeps it out of your
scripts and available in every session.}
}
\value{
A tibble with one row per period: \code{start} and \code{end} (date-times)
and \code{post_count} (integer). When the query matched nothing, the tibble
has the same three columns and no rows.
}
\description{
Returns how many posts in the full archive, back to 2006, match a search
query in each period via the \href{https://docs.x.com/x-api/posts/get-count-of-all-posts}{full-archive post counts endpoint}. The
endpoint needs pay-per-use or Enterprise access; on a tier without it the
call stops with the API's own message.

A count request is billed once (US$0.010 in September 2026), however many
posts it counts, and returns no posts at all. A long range comes back in
pages; the function follows them until the API has no more and prints how
many requests it made. Use it to size a query before paying for
\code{\link[=get_all_post]{get_all_post()}}.
}
\examples{
\dontrun{
counts <- get_all_post_count(
  "#SuperBowl lang:en",
  start_time = iso_8601("2020-01-01"),
  end_time = iso_8601("2020-03-01")
)
}
}
