% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_news.R
\name{get_news}
\alias{get_news}
\title{Get News Story by ID}
\usage{
get_news(
  news_id,
  bearer_token = Sys.getenv("X_BEARER_TOKEN"),
  news_fields = default_news_fields()
)
}
\arguments{
\item{news_id}{The story's id, as a string of digits. Keep ids as text:
as numbers they lose digits.}

\item{bearer_token}{A string containing the bearer token for authenticating
with the X API. By default, this argument retrieves the token from the
environment variable \code{X_BEARER_TOKEN} (via \code{Sys.getenv("X_BEARER_TOKEN")}).
Adding your bearer token to your \code{.Renviron} file keeps it out of your
scripts and available in every session.}

\item{news_fields}{\code{character}, \code{vector}; the fields to return
for each story. The default asks for everything the table reads.}
}
\value{
A tibble with one row and the columns described in
\code{\link[=search_news]{search_news()}}: \code{news_id}, \code{name}, \code{hook}, \code{summary}, \code{category},
\code{disclaimer}, \code{keywords} and \code{contexts} (list-columns) and \code{updated_at}
(POSIXct, UTC).
}
\description{
Retrieves one news story by its id via the
\href{https://docs.x.com/x-api/news/get-news-stories-by-id}{get news story by ID endpoint}.
An app bearer token is enough. An id the API cannot find stops the call
with the API's reason.

News is not on the X API pricing page as of 24 September 2026, so this
function prints no cost line.
}
\examples{
\dontrun{
story <- get_news("1989418137272422538")
}
}
