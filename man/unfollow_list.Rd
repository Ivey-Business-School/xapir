% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/follow_list.R
\name{unfollow_list}
\alias{unfollow_list}
\title{Unfollow List}
\usage{
unfollow_list(list_id)
}
\arguments{
\item{list_id}{The id of the list to unfollow, as a string.}
}
\value{
Invisibly, the \code{data} list the API returns, \code{list(following = FALSE)}. Stops with the API's message when the request is refused.
}
\description{
Makes the signed-in account unfollow a list via the \href{https://docs.x.com/x-api/users/unfollow-list}{unfollow list endpoint}. Needs a user
token, so the first call opens a browser window to sign in. The request
is billed, so the function says what it costs before it sends anything.
}
\examples{
\dontrun{
unfollow_list(list_id = "1146654567674912769")
}
}
