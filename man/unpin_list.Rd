% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pin_list.R
\name{unpin_list}
\alias{unpin_list}
\title{Unpin List}
\usage{
unpin_list(list_id)
}
\arguments{
\item{list_id}{The id of the list to unpin, as a string.}
}
\value{
Invisibly, the \code{data} list the API returns, \code{list(pinned = FALSE)}. Stops with the API's message when the request is refused.
}
\description{
Unpins a list from the signed-in account's lists via the \href{https://docs.x.com/x-api/users/unpin-list}{unpin list endpoint}. Needs a user token,
so the first call opens a browser window to sign in. The request is
billed, so the function says what it costs before it sends anything.
}
\examples{
\dontrun{
unpin_list(list_id = "1146654567674912769")
}
}
