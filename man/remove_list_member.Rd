% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_member.R
\name{remove_list_member}
\alias{remove_list_member}
\title{Remove List Member}
\usage{
remove_list_member(list_id, username = NULL, user_id = NULL)
}
\arguments{
\item{list_id}{The id of the list, as a string.}

\item{username}{Username of the account to remove, without the "@"
symbol.}

\item{user_id}{The id of the account to remove, as a string of digits.
When given, \code{username} must be \code{NULL}.}
}
\value{
Invisibly, the \code{data} list the API returns, \code{list(is_member = FALSE)}. Stops with the API's message when the request is refused.
}
\description{
Removes an account from a list the signed-in account owns via the \href{https://docs.x.com/x-api/lists/remove-list-member}{remove list member endpoint}.
Needs a user token, so the first call opens a browser window to sign in.
The request is billed, so the function says what it costs before it sends
anything.

Give either \code{username} or \code{user_id}, not both. A handle costs one user
read to turn it into an id before the member is removed.
}
\examples{
\dontrun{
remove_list_member(list_id = "1146654567674912769", username = "Tesla")
}
}
