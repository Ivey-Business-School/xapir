% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_communities.R
\name{search_communities}
\alias{search_communities}
\title{Search Communities}
\usage{
search_communities(
  query,
  max_results = 100,
  community_fields = default_community_fields()
)
}
\arguments{
\item{query}{One search string, matched against community names and
descriptions.}

\item{max_results}{The most communities to return, between 10 and 100.
Default 100. The function stops before any request if the value is
outside that range.}

\item{community_fields}{\code{character}, \code{vector}; the fields to
return for the community. The default asks for everything the table
reads.}
}
\value{
A tibble with one row per community and the columns described in
\code{\link[=get_community]{get_community()}}: \code{community_id}, \code{name}, \code{description}, \code{access},
\code{join_policy}, \code{member_count} and \code{created_at} (POSIXct, UTC). When
nothing matches, the same columns with no rows.
}
\description{
Finds communities whose name or description matches a search via the
\href{https://docs.x.com/x-api/communities/search-communities}{search communities endpoint}.
This endpoint needs the signed-in user's token, so the function calls
\code{\link[=authenticate_user]{authenticate_user()}} (a browser window opens the first time). Every
community returned is billed, so the function says what the call can
cost before it reads anything.
}
\examples{
\dontrun{
communities <- search_communities("marketing", max_results = 20)
}
}
