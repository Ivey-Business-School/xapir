% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_personalized_trends.R
\name{get_personalized_trends}
\alias{get_personalized_trends}
\title{Get Personalized Trends}
\usage{
get_personalized_trends()
}
\value{
A tibble with one row per trend: \code{trend_name}, \code{category},
\code{post_count} (integer, \code{NA} when the API gives a count it cannot parse
or none at all) and \code{trending_since} (the API's own text). When there
are no trends, the same columns with no rows.
}
\description{
Retrieves the trends X picks for the signed-in account via the \href{https://docs.x.com/x-api/trends/get-personalized-trends}{get personalized trends endpoint}. Needs
a user token, so the first call opens a browser window to sign in. The
request is billed, so the function says what it costs before it reads
anything.
}
\examples{
\dontrun{
trends <- get_personalized_trends()
}
}
