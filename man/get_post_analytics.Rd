% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_post_analytics.R
\name{get_post_analytics}
\alias{get_post_analytics}
\title{Get Post Analytics}
\usage{
get_post_analytics(post_ids, start_time, end_time, granularity = "total")
}
\arguments{
\item{post_ids}{A character vector of up to 100 post ids. They must be
posts by the account that signed in.}

\item{start_time}{The start of the period, as an ISO 8601 string such as
"2026-09-01T00:00:00Z" or a date-time object.}

\item{end_time}{The end of the period, in the same form.}

\item{granularity}{"total" (the default) for one row per post, or
"hourly", "daily" or "weekly" for one row per post per period.}
}
\value{
A tibble with one row per post per period: \code{post_id}, \code{timestamp}
(POSIXct, UTC; \code{NA} for a total) and one integer column per metric:
\code{impressions}, \code{engagements}, \code{likes}, \code{reposts}, \code{replies}, \code{quotes},
\code{bookmarks}, \code{follows}, \code{unfollows}, \code{url_clicks},
\code{user_profile_clicks}, \code{media_views}, \code{detail_expands},
\code{permalink_clicks}, \code{hashtag_clicks}, \code{shares}, \code{app_opens},
\code{app_install_attempts}, \code{email_post} and \code{unlikes}. The API calls
reposts \code{retweets}, quotes \code{quote_tweets} and email shares
\code{email_tweet}; the table uses the post names. When the API returns
nothing, the same columns with no rows.
}
\description{
Retrieves engagement metrics over time for up to 100 of the signed-in
account's own posts via the \href{https://docs.x.com/x-api/posts/get-post-analytics}{get post analytics endpoint}. Needs a
user token, so the first call opens a browser window to sign in.

The pricing page does not list this endpoint, so the function prints no
cost line. Check your usage on the developer console after the first
call.
}
\examples{
\dontrun{
daily <- get_post_analytics(
  post_ids    = c("1234567890123456789", "1234567890123456790"),
  start_time  = "2026-09-01T00:00:00Z",
  end_time    = "2026-09-08T00:00:00Z",
  granularity = "daily"
)
}
}
